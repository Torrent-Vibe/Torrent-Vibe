{
  "framework": "vite",
  "installCommand": "ELECTRON_SKIP_BINARY_DOWNLOAD=1 pnpm install",
  "buildCommand": "pnpm build:production",
  "outputDirectory": "layer/renderer/dist",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ]
}
